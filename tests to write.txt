tests to write:
- import crate track
- import full track
- import crate
- export crate
- export crate with new tracks
(including remove tracks)
- import db
- object lengths
- export bytes correctly
- ID3 timestamp import

questions:
- are imported songs being encoded correctly?
- will they be re-encoded if serato adds more tags?
- what is the minimum info needed for a serato to read a song?
- can you add a song to a crate if its not in the serato library? -- YES
- do you need crate info pre-loaded? -- NO